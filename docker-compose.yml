services:
  backend:
    image: node:18-alpine
    working_dir: /app
    volumes:
      - ./server:/app
    network_mode: host
    environment:
      - PORT=9999
      - NODE_ENV=production
    command: sh -c "npm install --silent 2>/dev/null; node server.js"
